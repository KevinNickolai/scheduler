const instantiateMessage = require('../instantiateMessage.js');

module.exports = (client, assert) => {
	describe('Test Join Command', function () {
		it('user joins a single event');

		it('user fails to join event twice');

		it('user fails to join nonexistent event');
	});
};